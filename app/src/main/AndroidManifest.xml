<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liceoatarraya.app">

    <application
        android:allowBackup="true"
        android:background="#2271b3"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".layout.calificaciones_activity"></activity>
        <activity android:name=".layout.calendario_activity" />
        <activity android:name=".layout.aulavirtual_activity" />
        <activity
            android:name=".layout.menuNavegacion"
            android:label="@string/title_activity_menu_navegacion"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".layout.Login" />
        <activity android:name=".layout.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".principalMenu" />
    </application>

</manifest>